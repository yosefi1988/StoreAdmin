@model IEnumerable<WebApplicationStoreAdmin.Models.SD_ProductChargesProperties>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SizeID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Discount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RemainingCount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SD_Color.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SD_ProductCharges.BuyInvoiceNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SizeID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Discount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RemainingCount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SD_Color.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SD_ProductCharges.BuyInvoiceNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
